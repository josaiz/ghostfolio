---
description: Generates/recalls the runbook to demonstrate the workshop happy path end-to-end (INT-01).
agent: workshop-facilitator-agent
---

Prepare the demo runbook for: **$ARGUMENTS** (if empty, the default happy path "Portfolio Insights Demo").

Reference runbook for the solution:
@docs/workshop/solution-runbook.md

Work as follows:
1. Summarize the **exact and verifiable steps** to demonstrate the full cycle:
   - Start Ghostfolio (`./scripts/start.sh` / `.\scripts\start.ps1`) and, if needed, seed demo data.
   - Start/verify the demo MCP (`./scripts/check-demo-mcp.sh` / `.\scripts\check-demo-mcp.ps1`).
   - Confirm that OpenCode sees commands/agents/skills/MCP.
   - Run `/workshop-analyze-demo-portfolio` and display the insights.
   - Pass the result through `/workshop-review-financial-safety`.
   - (Optional) show a plan generated by `/workshop-plan-frontend-card FE-01`.
2. Indicate the order, the exact command, and what should be visible at each step (success criterion).
3. Point out the fallbacks if something does not start (see `facilitator-guide.md`).

Output: a clear markdown runbook, ready to project. Save it to `docs/workshop/generated/demo-runbook.md` if requested.
